sudo gem install colorls

# edit this path with the correct version number;
# find the version number via `gem which colorls`
# or `gem env` if that doesn't find it

ln -s /opt/homebrew/lib/ruby/gems/3.4.0/bin/colorls /opt/hom
ebrew/bin/colorls

